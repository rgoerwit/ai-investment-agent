"""Provider-neutral LLM binding and runtime implementation.

Package roots intentionally expose no convenience imports. Import contracts from
their owning modules so dependency direction remains visible.
"""
